---
title: Web
description: Brug af NexusFlow i din browser.
---

NexusFlow kan køre som en webapplikation i din browser, hvilket giver den samme kraftfulde AI-kodningsoplevelse uden behov for en terminal.

![NexusFlow Web - Ny session](../../../assets/web/web-homepage-new-session.png)

## Kom godt i gang

Start webgrænsefladen ved at køre:

```bash
nexusflow web
```

Dette starter en lokal server på `127.0.0.1` med en tilfældig tilgængelig port og åbner automatisk NexusFlow i din standardbrowser.

:::caution
Hvis `NEXUSFLOW_SERVER_PASSWORD` ikke er indstillet, vil serveren være usikret. Dette er fint til lokal brug, men bør indstilles til netværksadgang.
:::

:::tip[Windows-brugere]
For den bedste oplevelse skal du køre `nexusflow web` fra [WSL](/docs/windows-wsl) i stedet for PowerShell. Dette sikrer korrekt filsystemadgang og terminalintegration.
:::

---

## Konfiguration

Du kan konfigurere webserveren ved hjælp af kommandolinjeflag eller i din [config-fil](/docs/config).

### Port

Som standard vælger NexusFlow en tilgængelig port. Du kan angive en port:

```bash
nexusflow web --port 4096
```

### Værtsnavn

Som standard binder serveren til `127.0.0.1` (kun lokal vært). Sådan gør du NexusFlow tilgængelig på dit netværk:

```bash
nexusflow web --hostname 0.0.0.0
```

Når du bruger `0.0.0.0`, vil NexusFlow vise både lokale adresser og netværksadresser:

```
  Local access:       http://localhost:4096
  Network access:     http://192.168.1.100:4096
```

### mDNS-opdagelse

Aktiver mDNS for at gøre din server synlig på det lokale netværk:

```bash
nexusflow web --mdns
```

Dette indstiller automatisk værtsnavnet til `0.0.0.0` og annoncerer serveren som `nexusflow.local`.

Du kan tilpasse mDNS-domænenavnet til at køre flere forekomster på det samme netværk:

```bash
nexusflow web --mdns --mdns-domain myproject.local
```

### CORS

For at tillade yderligere domæner til CORS (nyttigt til brugerdefinerede frontends):

```bash
nexusflow web --cors https://example.com
```

### Godkendelse

For at beskytte adgangen skal du angive en adgangskode ved hjælp af `NEXUSFLOW_SERVER_PASSWORD` miljøvariablen:

```bash
NEXUSFLOW_SERVER_PASSWORD=secret nexusflow web
```

Brugernavnet er som standard `nexusflow`, men kan ændres med `NEXUSFLOW_SERVER_USERNAME`.

---

## Brug af webgrænsefladen

Når den er startet, giver webgrænsefladen adgang til dine NexusFlow-sessioner.

### Sessioner

Se og administrer dine sessioner fra hjemmesiden. Du kan se aktive sessioner og starte nye.

![NexusFlow Web - Active Session](../../../assets/web/web-homepage-active-session.png)

### Serverstatus

Klik på "Se servere" for at se tilsluttede servere og deres status.

![NexusFlow Web - Se servere](../../../assets/web/web-homepage-see-servers.png)

---

## Tilslutning af en terminal

Du kan vedhæfte en terminal TUI til en kørende webserver:

```bash
# Start the web server
nexusflow web --port 4096

# In another terminal, attach the TUI
nexusflow attach http://localhost:4096
```

Dette giver dig mulighed for at bruge både webgrænsefladen og terminalen samtidigt og dele de samme sessioner og tilstand.

---

## Konfigurationsfil

Du kan også konfigurere serverindstillinger i din `nexusflow.json`-konfigurationsfil:

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "cors": ["https://example.com"]
  }
}
```

Kommandolinjeflag har forrang over konfigurationsfilindstillinger.
